package common.utils

import scala.collection.mutable

object GrokFactory {


//  val grokCompiler: GrokCompiler = initGrokConf
//
//  val map = mutable.HashMap[String, Grok]()
//
//  private def initGrokConf = {
//    val grokCompiler = GrokCompiler.newInstance
//    grokCompiler.registerDefaultPatterns()
//    grokCompiler.register("STREAM_URL", "(((rtmp)|(http))\\://([\\w-]+\\.)+[\\w-]+\\/[\\w-]+)")
//    grokCompiler
//  }
//
//  def getGrok(pattern: String): Grok = {
//    if (!map.contains(pattern)) {
//      map.put(pattern, grokCompiler.compile(pattern))
//    }
//    map.get(pattern).get
//  }


}
